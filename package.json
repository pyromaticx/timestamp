{
  "name": "fcc-microstamp-service",
  "version": "1.0.0",
  "description": "A microstamp service that returns both a unix and natural language date",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kelly Garrett",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3"
  }
}
